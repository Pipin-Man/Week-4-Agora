[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "HOST=0.0.0.0 PORT=${PORT:-8080} node ./dist/server/entry.mjs"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
